A lightweight alarm clock application built with Python.

The program prompts the user to enter a specific alarm time, monitors the system time at one-minute intervals, and activates the alarm once the current time matches the user-defined time.

Note: ⚠️ Sound playback currently works on macOS only.
